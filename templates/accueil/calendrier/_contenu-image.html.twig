{{~ Indentation }}<div id="{{ Jour }}" class="m-2 text-success bloc" title="Jour : {{ Jour }}" data-bordure="{{ Bordure }}" data-taille="{{ Taille }}" data-url="{{ path('Cadeau',{'Id': Jour}) }}">
{{~ Indentation }}  <img class="bloc {{ Bordure }} {{ Taille }}" src="{{ path('Accueil') }}{{ Application.getDossierImage() }}J{{ Jour }}.png">
{{~ Indentation }}</div>