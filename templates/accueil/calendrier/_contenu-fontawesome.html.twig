{%~ if Aleatoire == 0 -%}
  {% set Image = 'fa-gift' %}
{%~ else -%}
  {%~ if Jour in JourEtoile -%}
    {% set Image = 'fa-star' %}
  {%~ else -%}
    {% set Image = random(['fa-gift', 'fa-tree', 'fa-candy-cane', 'fa-holly-berry', 'fa-snowman', 'fa-snowflake', 'fa-sleigh', 'fa-igloo', 'fa-mitten', 'fa-bell']) %}
  {%~ endif -%}
{%~ endif -%}

{{~ Indentation }}<div id="{{ Jour }}" class="m-2 text-success bloc {{ Bordure }} {{ Taille }}" title="Jour : {{ Jour }}" data-taille="{{ Taille }}" data-url="{{ path('Resultat',{'id': Jour}) }}">
{{~ Indentation }}  <i class="fa-solid {{ Image }} text-danger affiche {{ Taille }}"></i>
{{~ Indentation }}  <strong class="m-0 chiffre {{ Taille }}" style="color:{{ Couleur }}">{{ Jour }}</strong>
{{~ Indentation }}</div>