{{~ Indentation }}<div class="m-2 text-success" data-style="{{ Style }}" data-classe="{{ Classe }}" data-url="{{ path('Cadeau',{'Id':Jour}) }}">
{{~ Indentation }}  <img class="Boule-{{ Classe }}" src="{{ path('Accueil') }}{{ Application.getDossierImage() }}J{{ Jour }}.png" title="Jour {{ Jour }}">
{{~ Indentation }}</div>