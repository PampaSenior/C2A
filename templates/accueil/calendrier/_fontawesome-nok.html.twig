{{~ Indentation }}<div class="m-2 text-success" data-style="{{ Style }}" data-taille="{{ Taille }}" data-url="{{ path('Cadeau',{'Id':Jour}) }}">
{{~ Indentation }}  <img class="Boule {{ Taille }}" src="{{ path('Accueil') }}{{ Application.getDossierImage() }}J{{ Jour }}.png" title="Jour {{ Jour }}">
{{~ Indentation }}</div>