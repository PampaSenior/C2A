{% for label, messages in app.flashes %}
  {%~ for message in messages %}
    {{~ Indentation }}<div class="m-2 alert alert-{{ label }} text-center" role="alert">
    {{~ Indentation }}  {{ message }}
    {{~ Indentation }}</div>
  {%~ endfor %}
{% endfor %}